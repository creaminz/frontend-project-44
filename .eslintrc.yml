env:
  node: true
  es2021: true

extends:
  - airbnb-base

parserOptions:
  ecmaVersion: latest
  sourceType: module

rules:
  # Здесь можно настроить специфические правила
  no-console: 0
  import/extensions: # Чтобы не ругался на .js в импортах
    - error
    - always
    - js: always